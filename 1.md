📦 部署到链上是做什么？
这两个命令的作用：
1. npm run node - 启动本地测试区块链

# 启动一个模拟的以太坊节点（Hardhat Network）
# 类似于本地运行一个"假的以太坊"
效果：

🔄 在本地运行一个以太坊节点（http://127.0.0.1:8545）
💰 自动创建20个测试账户，每个有10000 ETH
⚡ 即时挖矿（交易立即确认）
🔍 可以用MetaMask连接
输出示例：


Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/

Accounts:
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 (10000 ETH)
Account #1: 0x70997970C51812dc3A010C7d01b50e0d17dc79C8 (10000 ETH)
...
2. npm run deploy:local - 部署合约到本地节点

# 将你的4个合约部署到刚才启动的本地节点上
效果：

📝 编译合约
🚀 部署到本地节点
📍 返回合约地址
输出示例：


MessageVerifier deployed at: 0x5FbDB2315678afecb367f032d93F642f64180aa3
BetManager deployed at: 0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512
SettlementManager deployed at: 0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0
HTLCVault deployed at: 0xCf7Ed3AccA5a467e9e704C703E8D87F634fB0Fc9

问题1：部署是做什么？

将合约代码变成链上可执行的字节码, 获得合约地址（其他模块需要这个地址）


🔢 如何修改账户数量 ？
在 hardhat.config.ts 中添加配置：

3. 数量对功能有影响吗？
影响很小 ⚠️：

最少需要 5-6 个账户（部署者 + 3个Relayer + 玩家）
10个账户 - 足够开发测试 ✅
20个账户 - 更灵活，可模拟更多场景 ✅
核心功能不受影响 - 合约本身不关心账户数量


📋 deployed-addresses.json 的内容来源
我保存的是：
✅ 固定的默认地址 - Hardhat本地节点每次启动都会生成相同的账户
✅ 你刚才看到的合约地址 - 从你的部署输出中提取的
为什么地址是固定的？
Hardhat 使用固定的助记词（mnemonic），所以：


每次运行 npm run node
  ↓
生成的账户地址和私钥都是相同的
  ↓
Account #0 永远是 0xf39Fd...
Account #1 永远是 0x70997...


工作原理
结合配置文件中的 Relayer 设置：
relayers:
  - address: "0x70997970C51812dc3A010C7d01b50e0d17dc79C8"
    privateKey: "0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d"
  - address: "0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC"
    privateKey: "0x5de4111afa1a4b94908f83103eb1f1706367c2e68ca870fc3fb9a804cdab365a"
  - address: "0x90F79bf6EB2c4f870365E785982E1f101E93b906"
    privateKey: "0x7c852118294e51e653712a81e05800f419141751be58f605c371e15141b007a6"
3 个 Relayer 注册
至少需要 2 个签名（2-of-3 多签方案）
提高安全性，防止单点故障
这与合约的验证逻辑对应
在 MessageVerifier.sol 中，验证逻辑会检查：

收集到的签名数量 ≥ threshold
签名者都是已注册的 Relayer
签名有效且未重复
这是经典的多签验证方案，确保跨链消息的可信度。


===========================================================

┌─────────────────────────────────────────┐
│  开发/测试流程                            │
└─────────────────────────────────────────┘

第1步：启动本地区块链
  └─> npm run node
       (保持运行，不要关闭)

第2步：部署合约
  └─> npm run deploy:local
       (记录合约地址)

第3步：配置其他模块
  ├─> Relayer: 填入合约地址到 config.yaml
  ├─> SDK: 填入合约地址到配置
  └─> Frontend: 通过SDK连接合约

第4步：启动其他服务
  ├─> Relayer: go run main.go
  ├─> Frontend: npm run dev
  └─> 开始联调测试






🎯 实际操作步骤
终端1（保持运行）：

cd network
npm run node
# 输出合约地址后，复制账户私钥
终端2（部署一次）：

cd network
npm run deploy:local
# 复制输出的合约地址
终端3（配置Relayer）：

cd network/relayer
# 编辑 config.yaml，填入合约地址
vim config.yaml
终端4（启动Relayer）：

cd network/relayer
go run main.go
终端5（启动前端）：

cd frontend
npm run dev

窗口	命令
Chain A	npx hardhat node --port 8545
Chain B	set HARDHAT_CHAIN_ID=31338 然后 npx hardhat node --port 8546

====================================================
cd C:\Users\zhaow\Desktop\cross-chain\network

rem 部署 Chain A
npx hardhat run scripts/deploy.js --network localhost

rem 部署 Chain B
npx hardhat run scripts/deploy.js --network localhostB

rem 验证合约在链上
cd ..\sdk
node -e "const{ethers}=require('ethers');(async()=>{const p=new ethers.JsonRpcProvider('http://127.0.0.1:8545');const c=await p.getCode('0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512');console.log('A:',c.length>10?'OK':'FAIL');const p2=new ethers.JsonRpcProvider('http://127.0.0.1:8546');const c2=await p2.getCode('0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512');console.log('B:',c2.length>10?'OK':'FAIL');})()"

rem 两条都 OK 后，启动 relayer（另开窗口）
cd ..\relayer-python
python relayer_server.py

rem 最后跑测试
cd ..\sdk
npx ts-node test-integration.ts

======================================

Chain 31337                          Relayer                        Chain 31338
───────────                          ───────                        ───────────
下注 0.1 ETH ──► BetCreated ──► 监听到事件 ──► 签名(EIP-712) ──► registerRound
                   事件              确认 12 块      2-of-2 门限      roundId=1001 ✅

下注 0.1 ETH ──► 锁定资金 ──► 跨链 relay ──► round 注册
                                                      ↓
赢家收钱 ◄── executeSettlement ◄── relayer回传 ◄── 管理员开奖
BET STATUS: CLAIMED                                finalizeRound